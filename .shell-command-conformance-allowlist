# SPDX-FileCopyrightText: 2026 Mattia Egloff <mattia.egloff@pm.me>
# SPDX-License-Identifier: GPL-3.0-or-later
#
# Command-pair gaps in this shell that are correct rather than defects.
# Format: <command>  # why. The reason is mandatory.
#
# Checked by scripts/scripts/check-shell-command-conformance.py.

DismissOverlay  # PresentationHostEffects.cs raises a modal ContentDialog and the item-click handler calls dialog.Hide() before dispatching, so the overlay is already closed when Core's dismissal would arrive; the modal also blocks re-tapping the affordance, so the toggle case cannot arise. Verified 2026-08-09 by reading the handler.
