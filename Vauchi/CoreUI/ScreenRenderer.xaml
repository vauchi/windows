<!-- SPDX-FileCopyrightText: 2026 Mattia Egloff <mattia.egloff@pm.me> -->
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<UserControl
    x:Class="Vauchi.CoreUI.ScreenRenderer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Screen header: title + subtitle + progress -->
        <StackPanel x:Name="HeaderPanel" Grid.Row="0" Spacing="4" Margin="0,0,0,16">
            <!-- Core-driven back chrome: shown only when the screen reports
                 `can_go_back`; calls navigate_back. Replaces the footer "Back". -->
            <Button x:Name="BackButton" Visibility="Collapsed"
                    Background="Transparent" BorderThickness="0" Padding="0"
                    HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="nav_back">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xE72B;" FontSize="14" />
                    <TextBlock x:Name="BackButtonLabel" />
                </StackPanel>
            </Button>
            <TextBlock x:Name="ScreenTitle" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock x:Name="ScreenSubtitle" Style="{StaticResource SubtitleTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Visibility="Collapsed" />
            <ProgressBar x:Name="ProgressBar" Visibility="Collapsed" Minimum="0" />
            <TextBlock x:Name="ProgressLabel" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Visibility="Collapsed" />
        </StackPanel>

        <!-- Component area. Default: scrollable (layout = Scroll). When the
             ScreenModel `layout` is "Fixed" the code-behind reparents
             ComponentContainer directly into ContentHost (no ScrollViewer)
             so fixed screens (e.g. the QR exchange) don't reflow. -->
        <Grid x:Name="ContentHost" Grid.Row="1">
            <ScrollViewer x:Name="ContentScroller">
                <StackPanel x:Name="ComponentContainer" Spacing="16" />
            </ScrollViewer>
        </Grid>

        <!-- Screen-level action buttons (pinned to bottom, outside scroll) -->
        <StackPanel x:Name="ActionButtonPanel" Grid.Row="2"
                    Orientation="Horizontal" Spacing="8"
                    HorizontalAlignment="Right" Margin="0,16,0,0" />
    </Grid>
</UserControl>
